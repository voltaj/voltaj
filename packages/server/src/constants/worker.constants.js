const types = {
    PROGRESS: 'PROGRESS',
    ERROR: 'ERROR',
    DONE: 'DONE'
}

const status = {
    IDLE: 'IDLE',
    INACTIVE: 'INACTIVE',
    DEAD: 'DEAD'
}

module.exports = {
    types,
    status
}